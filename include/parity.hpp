#include <iostream>
#include <stdlib.h>
#include <vector>

using namespace std;

/**
 * @brief 
 * 
 * @param bits 
 * @param parity 
 * @return true 
 * @return false 
 */
bool parityChecker(vector<bool> bits, bool parity);
/**
 * @brief 
 * 
 * @param bits 
 * @param parity 
 * @return true 
 * @return false 
 */
bool addParity(vector<bool>& bits, bool parity);